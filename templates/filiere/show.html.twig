{% extends 'styles.html.twig' %}

{% block title %}Filiere{% endblock %}

{% block body %}
    <h1>Filiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Codefiliere</th>
                <td>{{ filiere.codefiliere }}</td>
            </tr>
            <tr>
                <th>Nomfiliere</th>
                <td>{{ filiere.nomfiliere }}</td>
            </tr>
            <tr>
                <th>Respfiliere</th>
                <td>{{ filiere.respfiliere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_filiere_index') }}">back to list</a>

    <a href="{{ path('app_filiere_edit', {'codefiliere': filiere.codefiliere}) }}">edit</a>

    {{ include('filiere/_delete_form.html.twig') }}
{% endblock %}
