{% extends 'base.html.twig' %}

{% block title %}Unite{% endblock %}

{% block body %}
    <h1>Unite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Codeunite</th>
                <td>{{ unite.codeunite }}</td>
            </tr>
            <tr>
                <th>Nomunite</th>
                <td>{{ unite.nomunite }}</td>
            </tr>
            <tr>
                <th>Coeficient</th>
                <td>{{ unite.coeficient }}</td>
            </tr>
            <tr>
                <th>Respunite</th>
                <td>{{ unite.respunite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_unite_index') }}">back to list</a>

    <a href="{{ path('app_unite_edit', {'codeunite': unite.codeunite}) }}">edit</a>

    {{ include('unite/_delete_form.html.twig') }}
{% endblock %}
