{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
    <h1>Note</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Note</th>
                <td>{{ note.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_note_index') }}">back to list</a>

    <a href="{{ path('app_note_edit', {'anneeuniversitaire': note.anneeuniversitaire}) }}">edit</a>

    {{ include('note/_delete_form.html.twig') }}
{% endblock %}
