{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
<center>
    <h1>Note</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Note</th>
                <td>{{ note.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_note_index') }}">Retour</a>

    <a href="{{ path('app_note_edit', {'anneeuniversitaire': note.anneeuniversitaire}) }}">Modifier</a>

    {{ include('note/_delete_form.html.twig') }}
</center>
{% endblock %}
