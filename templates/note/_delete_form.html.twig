<form method="post" action="{{ path('app_note_delete', {'anneeuniversitaire': note.anneeuniversitaire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note.anneeuniversitaire) }}">
    <button class="btn">Delete</button>
</form>
