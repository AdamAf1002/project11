<form method="post" action="{{ path('app_bac_delete', {'idbac': bac.idbac}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bac.idbac) }}">
    <button class="btn">Delete</button>
</form>
